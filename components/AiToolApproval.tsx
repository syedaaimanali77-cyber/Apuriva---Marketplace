// Spec 034 §5: a thin re-export of the design system's existing `ui/components/ai/AiToolApproval`, exactly as
// `components/StatBlock.tsx` does. Ask Apuriva is its first app consumer; no parallel AI primitive exists.
export { AiToolApproval } from '@/ui/components/ai/AiToolApproval';
export type { AiToolApprovalProps, ToolArg } from '@/ui/components/ai/AiToolApproval';
