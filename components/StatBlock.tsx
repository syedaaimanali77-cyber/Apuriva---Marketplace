// Spec 033 §5: a thin re-export of the design system's existing `ui/components/data/StatBlock`,
// exactly as `components/Table.tsx` does — the admin AI-usage screen is its first consumer.
export { StatBlock } from '@/ui/components/data/StatBlock';
export type { StatBlockProps } from '@/ui/components/data/StatBlock';
