Amber star rating with the numeric value beside it — the stars alone are never the only signal.

```jsx
<Rating value={4.8} count={126} />
```

Only verified completed bookings produce ratings (master spec §52).
