Navy banner that takes priority above everything else on Home while a booking is live.

```jsx
<ActiveBookingBanner status="Provider on the way" service="AC Repair" providerName="Ali Raza"
  when="Arrives ~4:55 PM" action={<Button variant="inverse" size="sm">Track</Button>} />
```
