The "Finding your provider" progress spine (master spec §37). Horizontal orientation is the compact header variant.

```jsx
<RequestStatusTimeline currentIndex={2} steps={[
  {label:'Request sent',detail:'4:32 PM'},{label:'Providers notified',detail:'8 providers'},
  {label:'Offers received',detail:'3 offers'},{label:'Provider selected'},{label:'Payment'},{label:'Booking confirmed'}]} />
```
