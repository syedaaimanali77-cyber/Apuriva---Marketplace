Nothing-here state that always offers a way forward.

```jsx
<EmptyState icon="search" title="We couldn't find a match nearby"
  suggestions={['Expand service area','Change date or time','Adjust budget']}
  action={<Button>Post a request</Button>} />
```
