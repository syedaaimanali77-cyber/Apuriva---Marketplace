Checkbox with an always-visible label and optional description. The whole row is a 44px touch target.

```jsx
<Checkbox label="Send me offer alerts" description="Only for requests you've posted." checked={on} onChange={...} />
```
