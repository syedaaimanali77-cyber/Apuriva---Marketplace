Native select styled to match Input — native is intentional: it gets platform keyboard and screen-reader behaviour for free.

```jsx
<Select placeholder="Any budget" options={[{value:'lt3',label:'Under Rs. 3,000'},{value:'3to7',label:'Rs. 3,000 – 7,000'}]} />
```
