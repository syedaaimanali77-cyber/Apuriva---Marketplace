Multi-line field for request descriptions, offer messages and review text.

```jsx
<Textarea rows={5} maxLength={600} value={desc} onChange={e => setDesc(e.target.value)} placeholder="Describe what you need" />
```
