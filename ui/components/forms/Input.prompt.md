Single-line text field. Always pair with `FormField` so it has a real label.

```jsx
<Input placeholder="Search services or providers" iconLeft="search" />
<Input prefix="Rs." inputMode="numeric" placeholder="3,000" />
<Input invalid aria-describedby="budget-error" />
```
