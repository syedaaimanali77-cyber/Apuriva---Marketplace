Provider/customer identity image with an initials fallback and optional verification tick.

```jsx
<Avatar name="Ali Raza" size="lg" verified />
<Avatar src={photoUrl} name="Sana Khalid" size="sm" />
```
