Square icon-only action. `label` is mandatory — it supplies the accessible name.

```jsx
<IconButton icon="heart" label="Save provider" />
<IconButton icon="funnel" label="Filters" variant="outline" />
```

Use `lg` (44px) for any icon-only control that is a primary touch target on mobile.
