Brand lockup. Pass `src` to use the supplied artwork; without it you get the wordmark set in Sora — never a redrawn mark.

```jsx
<Logo src="assets/apuriva-logo-full.jpeg" size={22} />
<Logo tone="light" tagline />
```
