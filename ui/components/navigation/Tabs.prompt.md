Section switcher within a screen — booking states, request states, admin queues.

```jsx
<Tabs activeId={t} onSelect={setT} items={[
  {id:'upcoming',label:'Upcoming',count:2},{id:'active',label:'Active'},{id:'completed',label:'Completed'}]} />
```
