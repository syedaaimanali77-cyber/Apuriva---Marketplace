import { PlaceholderPage } from '@/app/_components/PlaceholderPage';

export default function RequestsPage() {
  return (
    <PlaceholderPage
      title="Requests"
      description="Posting and tracking service requests is coming soon (spec 015). This nav destination is reachable now so the customer navigation shell (spec 014) has nowhere broken to link."
    />
  );
}
