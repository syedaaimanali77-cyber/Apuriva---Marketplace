import { PlaceholderPage } from '@/app/_components/PlaceholderPage';

export default function AdminAnalyticsPage() {
  return (
    <PlaceholderPage
      title="Analytics"
      description="Analytics reporting is coming soon (spec 040) — it aggregates data every earlier domain spec produces, so it's sequenced late. This nav destination is reachable now so the admin navigation shell (spec 014) has nowhere broken to link."
    />
  );
}
