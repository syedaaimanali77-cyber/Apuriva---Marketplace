import { PlaceholderPage } from '@/app/_components/PlaceholderPage';

export default function ProviderEarningsPage() {
  return (
    <PlaceholderPage
      title="Earnings"
      density="moderate"
      description="Payout and earnings tracking is coming soon (spec 024). This nav destination is reachable now so the provider navigation shell (spec 014) has nowhere broken to link."
    />
  );
}
