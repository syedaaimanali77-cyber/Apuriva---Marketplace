import { PlaceholderPage } from '@/app/_components/PlaceholderPage';

export default function ProviderDashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      description="The provider dashboard (earnings snapshot, activity overview) is coming soon (specs 024/037). This nav destination is reachable now so the provider navigation shell (spec 014) has nowhere broken to link."
    />
  );
}
