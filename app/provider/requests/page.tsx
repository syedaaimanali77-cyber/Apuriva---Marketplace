import { PlaceholderPage } from '@/app/_components/PlaceholderPage';

export default function ProviderRequestsPage() {
  return (
    <PlaceholderPage
      title="Requests"
      density="moderate"
      description="Viewing and responding to matched requests is coming soon (specs 015/017). This nav destination is reachable now so the provider navigation shell (spec 014) has nowhere broken to link."
    />
  );
}
