import { PlaceholderPage } from '@/app/_components/PlaceholderPage';

export default function BookingsPage() {
  return (
    <PlaceholderPage
      title="Bookings"
      description="Booking tracking is coming soon (spec 020). This nav destination is reachable now so the customer navigation shell (spec 014) has nowhere broken to link."
    />
  );
}
